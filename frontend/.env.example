# Copy to .env.local and adjust for your environment.
# Base URL of the backend API. In production, set this to your deployed
# Render (or other host) backend URL.
NEXT_PUBLIC_API_BASE=http://localhost:4000
